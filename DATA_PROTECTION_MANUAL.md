# 多语言云顶之弈数据防护手册

## 1. 手册目标
- 保障多语言 TFT 数据在匿名访问场景下的可用性与完整性。
- 提高恶意抓取成本，便于快速定位并封禁异常来源。
- 形成开发、运维、数据团队可复用的工作指引。

## 2. 数据流概览
1. 爬虫每日定时从英文站点抓取阵容/英雄/装备 JSON，统一写入暂存库。
2. 数据清洗与标准化：以 `slug`/`code` 绑定实体主键，写入正式主表与多语言表。
3. API 层对外提供 `/{locale}` 前缀接口，前端小程序携带 locale 请求对应语言数据。
4. Redis 等缓存服务缓存热点数据；监控服务实时收集请求指标与告警。

## 3. 多语言数据建模要点
- **主表 + Locale 表**：主表仅保存语言无关字段，多语言文案放入 `*_locale` 表，主键 (`entity_id`, `locale`)。
- **统一 slug**：所有上游（爬虫、脚本）只用英文 slug 作为唯一标识，避免语言差异导致关联失败。
- **版本字段**：实体表附带 `set_number`、`patch_version`，方便追溯历史与回滚。
- **别名管理**：`slug_alias` 表记录第三方来源的特殊写法，导入流程先映射再入库。

## 4. 匿名访问防护策略
### 4.1 令牌与签名
- 访问前调用 `POST /api/v1/tokens` 获取短期匿名 token，返回签名所需随机盐与过期时间。
- 所有业务接口需携带：`token`、`timestamp`、`nonce`、`signature=HMAC(token_secret, path+body+timestamp+nonce)`。
- token 绑定设备指纹/IP；可随时撤销或缩短有效期。

### 4.2 限速与配额
- 维度：IP、Token、设备指纹、接口类型。
- 默认阈值：示例 `GET 列表 60 req/min`、`GET 详情 120 req/min`；由配置中心在线调整。
- 超阈值策略：第一次返回 429 并写日志，重复触发自动拉黑 24h。

### 4.3 设备指纹
- 指纹生成：UA、屏幕尺寸、语言、存储标记（localStorage GUID）、运行环境等组合。
- 服务端维护 `fingerprint_profile`，统计行为分布，异常（高频、跨地区切换）即降级或封禁。

### 4.4 响应水印与扰动
- 在 JSON 中加入不可见字段（如 `_tokenHash`、字段顺序随机、列表轻微打乱但保证功能正确）。
- 针对可缓存的静态 JSON，生成带用户哈希的版本，若外泄可追踪来源。

### 4.5 动态挑战与蜜罐
- 当检测到异常行为：返回带挑战码的响应（如滑块/简单算术）要求客户端重新获取 token。
- 部署专用蜜罐接口（例如 `/api/v1/internal/ping`），正常客户端不会访问，若命中立即封禁来源并记录证据。

### 4.6 监控与告警
- 实时指标：QPS、命中率、429/5xx 比例、Top IP/指纹、平均响应大小。
- 告警规则：
  - 单 IP 5 分钟内请求量 > 平均 + 3σ。
  - token 重放（相同签名在短期内多次出现）。
  - 蜜罐/诱饵数据被拉取。
- 告警推送至飞书/Slack，同步创建工单。

## 5. 服务端实现清单
- [ ] 所有接口强制 HTTPS，拒绝 HTTP。
- [ ] 统一中间件验证 token、签名、时间戳（允许 ±60s 漂移）。
- [ ] 速率限制中间件支持多维度配额，阈值可热更新。
- [ ] Redis 缓存与限流都需设置 TTL，防止内存膨胀。
- [ ] 管理端提供黑白名单与 token 撤销功能。
- [ ] 日志规范：记录 `timestamp, ip, fingerprint, token_id, path, status, body_size`。

## 6. 客户端（小程序）配合
- 初始化时向 token 接口注册，存储 token 与指纹；切换语言不重建 token，减少暴露。
- 每次请求自动附带签名字段；签名逻辑使用混淆/拆分脚本，避免被简单提取。
- 缓存策略：仅缓存常用列表，TTL ≤ 10 分钟；敏感详情不持久化。
- 异常处理：收到 429/挑战码时主动等待/刷新 token，避免无限重试。

## 7. 运维与应急
1. **每日巡检**：检查限流命中、异常 IP、蜜罐访问记录。
2. **周度回顾**：汇总被封禁列表、token 重放次数、爬虫趋势，评估阈值与策略是否需调整。
3. **应急流程**：
   - 发现大规模爬取 → 立即冻结对应 token/IP/指纹。
   - 如为公开 CDN 泄露，切换水印版本并通知法务/数据团队。
   - 记录事件，生成 Postmortem，更新手册。

## 8. 开发自检清单
- [ ] 新增接口是否走统一鉴权与限速？
- [ ] 是否泄露内部字段（调试信息、SQL、Trace ID）？
- [ ] 缓存层是否按 locale/token 维度隔离？
- [ ] 监控项是否覆盖新接口？
- [ ] 客户端是否完成 token 轮换与异常处理逻辑？

---
本手册应随项目迭代定期更新，重大策略变更需经安全评审与运维确认后执行。
