# 定时任务配置说明

## ✅ 已配置的定时任务

### 爬虫定时任务
- **执行频率**: 每小时执行一次
- **执行时间**: 每个小时的第 0 分钟（例如：00:00, 01:00, 02:00...）
- **任务内容**: 自动爬取 TFTactics 团队组合数据并保存到数据库

## 📁 相关文件

### 1. `src/crawler/crawler.scheduler.ts`
定时任务服务，使用 `@Cron` 装饰器定义执行时间。

```typescript
@Cron(CronExpression.EVERY_HOUR) // 每小时执行一次
async handleCron() {
  // 执行爬虫任务
}
```

### 2. `src/app.module.ts`
已导入 `ScheduleModule` 并注册 `CrawlerScheduler` 服务。

## 🔧 修改定时任务频率

如果需要修改执行频率，可以编辑 `src/crawler/crawler.scheduler.ts` 文件：

### 常用 Cron 表达式

```typescript
// 每小时执行一次（当前设置）
@Cron(CronExpression.EVERY_HOUR)

// 每 30 分钟执行一次
@Cron('0 */30 * * * *')

// 每天凌晨 2 点执行
@Cron('0 0 2 * * *')

// 每周一凌晨 3 点执行
@Cron('0 0 3 * * 1')

// 每 5 分钟执行一次（测试用）
@Cron('*/5 * * * *')
```

### Cron 表达式格式

```
* * * * * *
│ │ │ │ │ │
│ │ │ │ │ └── 星期几 (0-7, 0 和 7 都表示周日)
│ │ │ │ └──── 月份 (1-12)
│ │ │ └────── 日期 (1-31)
│ │ └──────── 小时 (0-23)
│ └────────── 分钟 (0-59)
└──────────── 秒 (0-59, 可选)
```

## 📊 日志输出

定时任务执行时会输出日志：
- ✅ 成功: `定时任务执行成功: 爬取了 X 个团队组合`
- ❌ 失败: `定时任务执行失败: [错误信息]`

## 🚀 启动应用

应用启动后，定时任务会自动开始工作：

```bash
pnpm start:dev
```

## ⚠️ 注意事项

1. **应用必须持续运行**: 定时任务只在应用运行时生效，如果应用停止，定时任务也会停止。

2. **避免重复执行**: 定时任务和手动调用接口可能会同时执行，数据库服务已经处理了重复数据的问题。

3. **资源消耗**: 爬虫任务会使用 Puppeteer，消耗一定的系统资源。确保服务器有足够的内存和 CPU。

4. **错误处理**: 如果定时任务执行失败，不会影响应用的运行，只会记录错误日志。

## 🔍 测试定时任务

### 方法 1: 修改为更频繁的执行（测试用）
临时修改为每 5 分钟执行一次：
```typescript
@Cron('*/5 * * * *')
```

### 方法 2: 手动触发（开发用）
可以通过接口手动触发：
```bash
GET http://localhost:3000/crawler/tftactics
```

### 方法 3: 查看日志
启动应用后，在控制台查看定时任务的执行日志。

## 📝 扩展功能

如果需要添加更多定时任务，可以：

1. 在 `CrawlerScheduler` 中添加新的方法
2. 或创建新的 Scheduler 服务

例如，添加每天清理旧数据的任务：
```typescript
@Cron('0 0 3 * * *') // 每天凌晨 3 点
async cleanupOldData() {
  // 清理逻辑
}
```


